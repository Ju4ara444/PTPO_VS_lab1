// poisc_slov.h
// ============================================================
// Заголовочный файл. Здесь я ОБЪЯВЛЯЮ функции (говорю компилятору,
// что такие функции существуют и как их вызывать).
// Само тело функций лежит в poisc_slov.cpp.
//
// Лабораторная работа №1. Контейнеры STL.
// Этап 1: подсчёт слов + поиск.
// Этап 2: индексация позиций + поиск с позициями.
// Этап 3: работа с vector<int>.
// ============================================================

#ifndef POISC_SLOV_H     // Защита от двойного подключения.
#define POISC_SLOV_H     // Если файл уже подключали - всё, что ниже, пропустится.

#include <string>        // для работы со строками (string)
#include <map>           // для контейнера map (слово -> что-то)
#include <vector>        // для контейнера vector (динамический массив)

using namespace std;     // Чтобы писать string, а не std::string.

// ---------- ЭТАП 1: работа с файлами и словами ----------

// Убирает знаки препинания с краёв слова и делает все буквы маленькими.
// Например: "Свобода," -> "свобода",  "(ошибки)" -> "ошибки"
string slova_bez_zap(string slovo);

// Считает, сколько раз каждое слово встречается в файле.
// Возвращает map: ключ = слово, значение = сколько раз встретилось.
map<string, int> countWords(string filename);

// Печатает результат подсчёта: "слово - количество"
void printCount(map<string, int> slova);

// Ищет одно конкретное слово в файле и возвращает количество вхождений.
int findWord(string filename, string what);

// Печатает результат поиска (используется редко, но пусть будет).
void printFindResult(string what, int count);

// ---------- ЭТАП 2: позиции слов ----------

// Запоминает, на каких позициях стоит каждое слово в файле.
// Позиция = номер слова в тексте (нумерация с нуля, как в задании).
map<string, vector<int> > getWordPositions(string filename);

// Печатает позиции всех слов в формате: "слово – 0, 5, 12, ..."
void printPositions(map<string, vector<int> > pozicii);

// Ищет одно слово в файле и возвращает СПИСОК его позиций.
// (В отличие от findWord, которая возвращает только количество.)
vector<int> findWordPositions(string filename, string what);

// Печатает список позиций. Если их больше 10 - показывает первые 10.
void printFoundPositions(vector<int> nomera);

// ---------- ЭТАП 3: работа с vector<int> ----------

// Проверяет, является ли число простым.
// Простое число делится только на 1 и на само себя (2, 3, 5, 7, 11, ...).
bool isProstoe(int chislo);

// Пункт 3а: проходит по вектору и заменяет все простые числа на их квадраты.
// Передаётся по ссылке (&), чтобы изменить оригинал, а не копию.
void kvadratProstyh(vector<int>& chisla);

// Пункт 3б: сортирует вектор так:
//   сначала нечётные по возрастанию, затем чётные по убыванию.
void sortNechetChet(vector<int>& chisla);

// Пункт 3в: возвращает УНИКАЛЬНЫЕ числа из диапазона [low, high],
// отсортированные по возрастанию.
vector<int> naydiVDiapazone(vector<int> chisla, int low, int high);

// Печатает вектор в одну строку через пробел.
void pechatVector(vector<int> chisla);

#endif   // Конец защиты от двойного подключения.